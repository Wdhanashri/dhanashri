# D19 · Two speeds on one track

**By the end of today:** you'll own Floyd's algorithm — and spot it even when the problem hides it inside an array.

⏱ ~120 min · Paper out again today.

---

## ⓪ Cold open — 10 min. Notes CLOSED.

1. The **four reversal lines**, in order.
2. Dummy node — when do you use one, and what do you return?
3. *(D12)* C-12's anchor. *(D3)* Prefix/suffix in one sentence.

> Mark ✅ / ⚠️ / ❌. ❌ → 🔴.

---

## ① Concept C-19 · Fast & slow pointers

**Anchor:** *"Two speeds on one track — they meet inside the loop."*
**Yaad rakho:** *"Do chaal, ek raasta — chakkar hai to milenge zaroor."*

```
slow  →   moves 1 step
fast  →→  moves 2 steps
```

**Why they must meet if there's a cycle** — you have to be able to say this out loud:

> Once both pointers are inside the loop, `fast` closes the gap by **exactly 1 step per move**. A gap that shrinks by 1 each time must reach 0. It cannot be jumped over.

No cycle? `fast` simply runs off the end and hits `None`.

```python
def has_cycle(head):
    slow = fast = head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow is fast:
            return True
    return False
```

### Three jobs, one tool

```
① CYCLE?        do they ever meet?

② MIDDLE        when fast reaches the end, slow is at the middle
                └─ this is step 1 of Reorder List, from yesterday

③ CYCLE START   after they meet, reset ONE pointer to head,
                then walk both at speed 1 — they meet at the entrance
```

```python
def middle(head):
    slow = fast = head
    while fast and fast.next:
        slow, fast = slow.next, fast.next.next
    return slow
```

---

### ✍️ Blank page — 4 min

> Close this. Write `has_cycle` from memory. Then write, in your own words, **why the gap closing by 1 per step forces a meeting.**
> That sentence matters more than the code.

---

## ② The disguise · Find the Duplicate Number

An **array** of `n+1` numbers in range `1..n`. Find the duplicate. Can't modify the array, must use O(1) space.

Nothing about this looks like a linked list. That's the entire test.

**The reframe:** treat `i → nums[i]` as a "next pointer."

```
nums = [1, 3, 4, 2, 2]
index:  0  1  2  3  4

start at 0 →  nums[0]=1 →  nums[1]=3 →  nums[3]=2 →  nums[2]=4 →  nums[4]=2 → ...
                                             ↑___________________________|
                                       a cycle — and its ENTRANCE is the duplicate
```

Two numbers point to the same place, which is precisely what creates a cycle. So: **Floyd's, then find the cycle start.**

```python
def find_duplicate(nums):
    slow = fast = 0
    while True:                      # phase 1: find the meeting point
        slow = nums[slow]
        fast = nums[nums[fast]]
        if slow == fast:
            break

    slow2 = 0                        # phase 2: find the entrance
    while slow != slow2:
        slow = nums[slow]
        slow2 = nums[slow2]
    return slow
```

**The transferable move — this is the real lesson of today:** *"is there a hidden next-pointer here?"* Any time you have "index → value → index", you have a linked list wearing a costume.

---

## ③ Now solve — 55 min

*Linked List* on **neetcode.io/practice**.

| NeetCode | Problem | Ask yourself | Cap |
|---|---|---|---|
| LL 4 | Remove Nth From End | *How far ahead should fast start? What handles removing the head?* | 20m |
| LL 7 | Linked List Cycle | *Five lines. Should be quick now.* | 10m |
| LL 8 | Find the Duplicate | *Where's the hidden next-pointer?* | 30m |

---

## ④ Teach-back — 5 min

> Out loud, 2 minutes: **"How does Floyd's cycle detection work, and why must the two pointers meet?"**

This is asked verbatim in interviews. Get it fluent now.

---

## ⑤ Ledger — 2 min

> Mark C-19.

**Tomorrow, cold:**
- C-19's anchor
- Why must fast and slow meet?
- *(D18)* Dummy node — when?

**Tomorrow: no new concepts.** Pure building — including LRU Cache, which interviewers love.
